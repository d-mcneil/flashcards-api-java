get decks
create decks
    /decks

get cards
create cards
    /decks/{deckId}/cards

get settings
update settings
    /decks/{deckId}/practice-settings

update decks
delete decks
    /decks/{deckId}

update cards
delete cards
    decks/{deckId}/cards/{cardId}

delete user
    /users

create decks
    decks

registration and login (POST)
    /register
    /sign-in
